"use strict";
{
    globalThis.C3.Plugins.EMI_INDO_plugin_android_imei.Instance = class plugin_android_imeiInstance extends globalThis.ISDKInstanceBase {
        constructor() {
            super();

            const properties = this._getInitProperties();


            if (properties) { }

            this.eventResponse = "";
        }

        _release() {
            super._release();
        }

        _saveToJson() {
            return {
                // data to be saved for savegames
            };
        }

        _loadFromJson(o) {
            // load state for savegames
        }


    };
}